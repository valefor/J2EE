<?xml version="1.0" encoding="UTF-8"?>

<!-- Created by ZK Studio -->

<zk>

	<!-- clustering environment Config -->
	<!-- Disable event thread, that you may not be able to use zk modal window -->
	<system-config>
<!-- 		<disable-event-thread /> -->
		<ui-factory-class>org.zkoss.zk.ui.http.SerializableUiFactory
		</ui-factory-class>
	</system-config>
	<!-- clustering environment (available since ZK 5.0.8) -->
	<listener>
		<listener-class>org.zkoss.zkplus.cluster.ClusterSessionPatch
		</listener-class>
	</listener>
	<!-- [Required if ZK 5] disable the use of LogManager (prohibited by SAE). -->
	<library-property>
		<name>org.zkoss.util.logging.hierarchy.disabled</name>
		<value>true</value>
	</library-property>

	<!-- Customized web app initiator -->
	<listener>
		<listener-class>org.alc.zk.MyAppInit</listener-class>
	</listener>

	<!-- GAE doesn't allow user's thread -->
	<client-config>
		<resend-delay>-1</resend-delay>
	</client-config>

	<device-config>
		<device-type>ajax</device-type>
		<timeout-uri>/timeout.zul</timeout-uri><!-- An empty URL can cause the 
			browser to reload the same URL -->
	</device-config>


	<!-- Error Pages -->
	<error-page>
		<exception-type>org.springframework.security.access.AccessDeniedException</exception-type>
		<location>/WEB-INF/errors/handler/accessDeniedHandler.zul</location>
	</error-page>
	<!-- <system-config> <label-location>/WEB-INF/labels/links.properties</label-location> 
		</system-config> -->

	<system-property>
		<name>org.zkoss.zel.impl.parser.SKIP_IDENTIFIER_CHECK</name>
		<value>true</value>
	</system-property>
</zk>
